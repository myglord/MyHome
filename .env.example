# My Home – Frontend environment (optional)
# Copy to .env and set values when you need to override defaults.
# Vite only exposes variables prefixed with VITE_ to the client.

# API base URL (default: proxied via Vite to http://localhost:3001)
# VITE_API_URL=http://localhost:3001

# Backend API is configured in server/.env – see server/.env.example
